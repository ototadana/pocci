java:
  image: ototadana/jenkins-slave-java
  volumes:
    - /var/jenkins_home
  env_file:
    - ./java.env
    - ./.env
  external_links:
    - poccis_sonarqube_1:sonar
    - poccis_sonarqubedb_1:sonardb
    - poccis_portal_1:server
    - poccis_jenkins_1:jenkins
  environment:
    - JENKINS_URL=http://jenkins:8080/jenkins

nodejs:
  image: ototadana/jenkins-slave-nodejs
  volumes:
    - /var/jenkins_home
  env_file:
    - ./nodejs.env
    - ./.env
  external_links:
    - poccis_sonarqube_1:sonar
    - poccis_sonarqubedb_1:sonardb
    - poccis_portal_1:server
    - poccis_jenkins_1:jenkins
  environment:
    - JENKINS_URL=http://jenkins:8080/jenkins
