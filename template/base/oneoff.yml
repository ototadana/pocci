iojs:
  image:        ototadana/jenkins-slave-iojs
  privileged:   true
  working_dir:  /app
  volumes:
    - ${PWD}:/app
  external_links:
    - poccis_nginx_1:server
    - poccis_openldap_1:ldap
  env_file:
    - ./.env

java:
  image:        ototadana/jenkins-slave-java
  privileged:   true
  working_dir:  /app
  volumes:
    - ${PWD}:/app
  external_links:
    - poccis_nginx_1:server
    - poccis_openldap_1:ldap
  env_file:
    - ./.env

nodejs:
  image:        ototadana/jenkins-slave-nodejs
  privileged:   true
  working_dir:  /app
  volumes:
    - ${PWD}:/app
  external_links:
    - poccis_nginx_1:server
    - poccis_openldap_1:ldap
  env_file:
    - ./.env
